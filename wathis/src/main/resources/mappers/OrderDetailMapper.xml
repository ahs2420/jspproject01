<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.OrderDetailMapper">
	<select id="getAll" resultType="orderdetailvo">
		SELECT 
				id,
		member_id,
		order_id,
		product_id,
		option_id,
		option_name,
		amount,
		price,
		reg_date,
		FROM
			tb_order_detail
		ORDER BY id DESC
	</select>
	<select id="getCount" resultType="int">
	
	</select>
	<select id="getList" resultType="orderdetailvo">
	
	</select>
	<select id="getOne" resultType="orderdetailvo">
	
	</select>
	<insert id="setItem">
		INSERT INTO tb_order_detail
		SET
			id=#{id},
			member_id=#{member_id},
			order_id=#{order_id},
			product_id=#{product_id},
			option_id=#{option_id},
			option_name=#{option_name},
			amount=#{amount},
			price=#{price},
			reg_date=#{reg_date}
	</insert>
	<update id="updateItem">
		UPDATE SET tb_order_detail
			id=#{id},
			member_id=#{member_id},
			order_id=#{order_id},
			product_id=#{product_id},
			option_id=#{option_id},
			option_name=#{option_name},
			amount=#{amount},
			price=#{price},
			reg_date=#{reg_date}
		WHERE id=#{id}
	</update>
	<delete id="deleteItem">
		DELETE FROM tb_order_detail
		WHERE id=#{id}
	</delete>
</mapper>